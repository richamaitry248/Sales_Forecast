# Sales_Forecast
An automated, end-to-end sales forecasting engine using a multi-model "Tournament" (XGBoost, LSTM, Prophet, ARIMA) to generate high-precision 8-week forecasts, deployed via a FastAPI REST service.
